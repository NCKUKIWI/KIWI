<% layout('../public/layout') %>
  <div class="container">
    <div class="row">
      <div class="col-md-3 col-sm-3">
        <% include ../public/sidebar %>
      </div>
      <div class="col-md-9 col-sm-9">
        <div id="content">
          <table class="table" id="post-table">
            <tr>
              <th class="course_name orderbtn" data-name="course_name">課程名稱<i class='chevron down icon'></i></th>
              <th class="catalog orderbtn" data-name="catalog">類別</th>
              <th class="teacher orderbtn" data-name="teacher">開課老師</th>
              <th class="semester orderbtn" data-name="semester">學期</th>
            </tr>
            <% if(data.length == 0 ){ %>
              <tr>
                <th class="h3 text-center" colspan="4">查無資料</th>
              </tr>
            <% }else{ %>
              <% for(var i in data){ %>
                <tr id="post-<%= data[i].id %>" class="post-row" data-toggle="modal" data-target="#post-modal" data-id="<%= data[i].id %>" >
                  <td class="course_name">
                    <% if(user && (user.id == data[i].user_id) ){ %>
                      <span class="label label-pill label-success">My post</span>
                    <%  } %>
                    <%- data[i].course_name %>
                  </td>
                  <td class="catalog">
                    <%- data[i].catalog %>
                  </td>
                  <td class="teacher">
                    <%- data[i].teacher %>
                  </td>
                  <td class="semester">
                    <%- data[i].semester %>
                  </td>
                </tr>
              <% } %>
            <% } %>
          </table>
        </div>
      </div>
    </div>
  </div>

  <div class="modal fade" id="post-modal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
      </div>
    </div>
  </div>
